---
import { Image } from "@astrojs/image/components";
import Container from "@components/container.astro";
import Sectionhead from "@components/sectionhead.astro";
import Layout from "@layouts/Layout.astro";
import { getPets } from "@lib/api";

const {pets} = (await getPets());

---

<Layout title="Наши Подопечные">
  <Container>
    <Sectionhead>
      <Fragment slot="title">Наши Подопечные</Fragment>
      <Fragment slot="desc">
        Группа занимается помощью бездомным животным г. Калининграда и Калининградской области.
За редким исключением это могут быть домашние животные.
        </Fragment>
    </Sectionhead>
    <main class="mt-16 mb-16">
      <ul class="flex flex-wrap justify-between">
        {
          pets.map((pet) => (
            <li class="mb-8">
              <a href={`/pets/${pet.id}`}>
                    <Image
                      src={pet.picture.url}
                      alt={pet.name}
                      width="auto"
                      height="200"
                      format="avif"
                      class="w-full rounded-md"
                    />


                    <h3 class="text-2xl font-semibold leading-snug tracking-tight mt-1 ">
                      {pet.name}
                    </h3>

              </a>
            </li>
          ))
        }
      </ul>
    </main>
  </Container>
</Layout>
